apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubemulticlient
spec:
  replicas: 1
  selector:
    matchLabels:
      component: web
  template:
    metadata:
      labels:
        component: web
    spec:
      containers:
      - name: kubemulticlient
        image: ashutoshpandey/kubemulticlient
        ports:
        - containerPort: 5000